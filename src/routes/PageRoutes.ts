const PageRoutes = {
  home: '/',
  dashboard: '/dashboard',
  dashboardId: '/dashboard/:dashboard_id',
  dashboardChatbot: '/dashboard/chatbot/:dashboard_id',
  login: '/login',
  settings: '/settings',
  dashboardSettings: '/dashboard-settings/:dashboard_id',
};

export default PageRoutes;
